 
Page({
  data:{
      name:""
  },

  save() {
   //    console.log(this.data.name);
      wx.setStorageSync("name", {name:this.data.name, sex:'女'});
  },
  fetch() {
      let v = wx.getStorageSync('name');
      if(v) {
         console.log(v);
      }else {
          
      }
   }

})
